[package]
name = "paperless-api-integration-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
paperless-api = { path = ".." }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.50", features = ["macros", "rt", "sync", "time"] }
